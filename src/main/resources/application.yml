jwt:
  secret: abcdefghabcdefghabcdefghabcdefghabcdefghabcdefefeq
member:
  url: ${MEMBER_URL}
  port: ${MEMBER_PORT}
spring:
  redis:
    host: ${REDIS_URL}
    port: ${REDIS_PORT}
server:
  port: ${SERVER_PORT}